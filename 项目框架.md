## **项目名称：HappyLandlord-PC-Agent**

### **一、 整体架构图**

Code snippet

graph TD  
    A\[WeChat Mini-program\] \--\>|Screen Capture| B(Perception Layer: CV)  
    B \--\>|State Vector| C(Logic Layer: State Machine)  
    C \--\>|Hand/History Data| D(Decision Layer: AI Engine)  
    D \--\>|Action Strategy| E(Execution Layer: Script)  
    E \--\>|Mouse Events| A

## ---

**二、 核心模块详细设计**

### **1\. 感知层 (Perception Layer \- CV识别)**

这是项目的“眼睛”。PC端画面相对固定，我们可以组合使用**模板匹配**和**颜色过滤**。

* **窗口定位：** 使用 pygetwindow 定位微信小程序窗口，获取其 (x, y, width, height)。  
* **图像预处理：**  
  * 使用 mss 库（比 OpenCV 原生截屏快得多）进行区域截图。  
  * 对牌面区域进行灰度化和二值化，突出数字和花色。  
* **牌面识别逻辑：**  
  * **手牌识别：** 由于手牌会重叠，建议识别每张牌左上角的“数字+花色”特征点。  
  * **动作识别：** 监控屏幕中央特定位置，识别“叫地主”、“出牌”、“不出”等按钮是否出现（确定当前回合状态）。  
  * **他人出牌：** 监控左侧和右侧的固定区域，捕捉对方打出的牌。

### **2\. 逻辑状态机 (State Management)**

AI 引擎需要一个完整的“世界观”，你需要维护一个当前牌局的**状态类 (GameState)**：

* **外部信息：** 地主是谁、当前倍数、各玩家剩余手牌数。  
* **动态记录：** 维护一个 history 列表，记录从第一手开始所有玩家打出的所有牌（记牌器功能）。  
* **状态转换：** \* WAITING (等待开局)  
  * CALLING (叫分阶段)  
  * PLAYING (出牌阶段)  
  * ENDING (结算)

### **3\. 决策层 (Decision Layer \- AI引擎)**

对于初学者，**强烈建议直接接入 [DouZero](https://github.com/kwai/DouZero)**。

* **原理：** 它通过强化学习训练，将斗地主模拟成一个决策树，输入当前的“手牌 \+ 历史出牌 \+ 队友出牌”，输出“最优出牌组合”。  
* **数据转换桥梁：** 你需要写一个 Adapter，将 CV 识别出的字符（如 \['10', 'J', 'Q', 'K', 'A'\]）转换成 DouZero 模型需要的数值编码。

### **4\. 执行层 (Execution Layer \- 自动化操作)**

将 AI 的决策转化为物理点击。

* **选牌逻辑：** 这是最麻烦的一步。由于选牌需要点击牌的中心，而牌的数量变化会导致间距变化。  
  * **公式计算：** 根据识别到的手牌总数，计算第一张牌和最后一张牌的坐标，推算每张牌的中心点。  
* **模拟操作：** 使用 pyautogui 或 win32api。  
  * **拖拽选牌：** 如果要出连续的多张牌，模拟从左侧牌滑动到右侧牌。  
  * **防封策略：** 加入 random.uniform(0.1, 0.5) 的随机延迟，模拟真实人类点击。

## ---

**三、 推荐技术栈**

* **开发语言：** Python 3.10+  
* **UI 自动化：** pyautogui, pygetwindow  
* **图像处理：** OpenCV-Python (模板匹配/边缘检测)  
* **高速截屏：** mss  
* **深度学习框架：** PyTorch (用于运行 DouZero 模型)  
* **数据处理：** NumPy, Pandas

## ---

**四、 关键难点突破建议**

### **1\. 处理 DPI 缩放**

微信 PC 版在不同显示器（4K 或 1080P）上的缩放比例不同。

* **对策：** 在程序初始化时，自动获取 Windows 系统的缩放比例，并对点击坐标进行补偿转换。

### **2\. 解决“牌面重叠”下的识别**

手牌多的时候会挤在一起，只露出一小部分。

* **对策：** 针对牌的左上角（数字和花色最清晰的部分）建立模板库。对于“10”，由于占位较宽，需要特殊处理。

### **3\. 识别出牌区域的动态变化**

别人出牌后，由于牌数不同，显示区域的中心点会偏移。

* **对策：** 确定左右两侧玩家出牌的固定边界盒（Bounding Box），在该区域内使用 findContours 寻找白色色块，确定是否有新牌打出。

